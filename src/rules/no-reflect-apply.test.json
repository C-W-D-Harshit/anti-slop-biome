{
  "valid": [
    "const value = operation.apply(owner, args);",
    "Reflect.get(owner, key);"
  ],
  "invalid": [
    { "code": "const value = Reflect.apply(operation, owner, args);", "errors": 1 },
    { "code": "const value = Reflect['apply'](operation, owner, args);", "errors": 1 }
  ],
  "divergences": {
    "extraInvalid": [
      {
        "code": "const Reflect = { apply() { return 1; } }; Reflect.apply();",
        "reason": "GritQL has no scope resolution; a local binding shadowing Reflect is flagged."
      },
      {
        "code": "function invoke(Reflect: { apply(): number }) { return Reflect.apply(); }",
        "reason": "GritQL has no scope resolution; a parameter shadowing Reflect is flagged."
      }
    ]
  }
}
